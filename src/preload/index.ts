import { contextBridge,ipcRenderer } from 'electron'
import { IPC,type DownloadApi,type DownloadItem } from '../shared/download'
const api:DownloadApi={version:2,list:()=>ipcRenderer.invoke(IPC.list),add:(url,queued=false)=>ipcRenderer.invoke(queued?IPC.enqueue:IPC.startNow,url),enqueue:url=>ipcRenderer.invoke(IPC.enqueue,url),pause:id=>ipcRenderer.invoke(IPC.pause,id),resume:id=>ipcRenderer.invoke(IPC.resume,id),cancel:id=>ipcRenderer.invoke(IPC.cancel,id),startQueue:()=>ipcRenderer.invoke(IPC.startQueue),stopQueue:()=>ipcRenderer.invoke(IPC.stopQueue),openFolder:()=>ipcRenderer.invoke(IPC.openFolder),onChanged:listener=>{const handler=(_event:Electron.IpcRendererEvent,items:DownloadItem[])=>listener(items);ipcRenderer.on(IPC.changed,handler);return()=>ipcRenderer.removeListener(IPC.changed,handler)}}
contextBridge.exposeInMainWorld('downloads',api)
