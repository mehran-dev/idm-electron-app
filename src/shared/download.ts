export type DownloadStatus='queued'|'downloading'|'paused'|'interrupted'|'completed'|'cancelled'|'failed'
export interface DownloadItem{id:string;url:string;fileName:string;savePath:string;status:DownloadStatus;receivedBytes:number;totalBytes:number;speed:number;createdAt:string;queueId?:string;error?:string}
export interface DownloadQueue{id:string;name:string;concurrency:number;createdAt:string}
export interface DownloadApi{version:number;list():Promise<DownloadItem[]>;add(url:string,queued?:boolean,queueId?:string):Promise<DownloadItem>;enqueue(url:string,queueId?:string):Promise<DownloadItem>;pause(id:string):Promise<void>;resume(id:string):Promise<void>;cancel(id:string):Promise<void>;deleteCompleted():Promise<void>;listQueues():Promise<DownloadQueue[]>;createQueue(name:string,concurrency:number):Promise<DownloadQueue>;updateQueue(id:string,name:string,concurrency:number):Promise<void>;deleteQueue(id:string):Promise<void>;startQueue(id:string):Promise<void>;stopQueue(id:string):Promise<void>;openFolder():Promise<void>;onChanged(listener:(items:DownloadItem[])=>void):()=>void}
export const IPC={list:'downloads:list',add:'downloads:add',startNow:'downloads:start-now',enqueue:'downloads:enqueue',pause:'downloads:pause',resume:'downloads:resume',cancel:'downloads:cancel',deleteCompleted:'downloads:delete-completed',listQueues:'queues:list',createQueue:'queues:create',updateQueue:'queues:update',deleteQueue:'queues:delete',startQueue:'queues:start',stopQueue:'queues:stop',changed:'downloads:changed',openFolder:'downloads:open-folder'} as const
