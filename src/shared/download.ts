export type DownloadStatus='queued'|'downloading'|'paused'|'interrupted'|'completed'|'cancelled'|'failed'
export interface DownloadItem{id:string;url:string;fileName:string;savePath:string;status:DownloadStatus;receivedBytes:number;totalBytes:number;speed:number;createdAt:string;error?:string}
export interface DownloadApi{version:number;list():Promise<DownloadItem[]>;add(url:string,queued?:boolean):Promise<DownloadItem>;enqueue(url:string):Promise<DownloadItem>;pause(id:string):Promise<void>;resume(id:string):Promise<void>;cancel(id:string):Promise<void>;startQueue():Promise<void>;stopQueue():Promise<void>;openFolder():Promise<void>;onChanged(listener:(items:DownloadItem[])=>void):()=>void}
export const IPC={list:'downloads:list',add:'downloads:add',startNow:'downloads:start-now',enqueue:'downloads:enqueue',pause:'downloads:pause',resume:'downloads:resume',cancel:'downloads:cancel',startQueue:'downloads:start-queue',stopQueue:'downloads:stop-queue',changed:'downloads:changed',openFolder:'downloads:open-folder'} as const
