import { app,BrowserWindow,clipboard,dialog,ipcMain,shell } from 'electron'
import { existsSync,readFileSync,unlinkSync,writeFileSync } from 'node:fs'
import { basename } from 'node:path'
import { IPC } from '../../../shared/download'
import type { DownloadService } from '../../application/download-service'
export function registerDownloadHandlers(service:DownloadService){ipcMain.handle(IPC.list,()=>service.list());ipcMain.handle(IPC.add,(_e,url:string,queueId?:string,segments?:number)=>service.enqueue(url,queueId,segments));ipcMain.handle(IPC.enqueue,(_e,url:string,queueId?:string,segments?:number)=>service.enqueue(url,queueId,segments));ipcMain.handle(IPC.startNow,(_e,url:string,segments?:number)=>service.add(url,segments));ipcMain.handle(IPC.importList,async(_e,queueId:string)=>{const picked=await dialog.showOpenDialog({title:'Import download list',buttonLabel:'Open',properties:['openFile'],filters:[{name:'Text URL lists',extensions:['txt']},{name:'All files',extensions:['*']}]});if(picked.canceled||!picked.filePaths[0])return{fileName:'',imported:0,skipped:0,cancelled:true};const path=picked.filePaths[0],lines=readFileSync(path,'utf8').replace(/^\uFEFF/,'').split(/\r?\n/);let imported=0,skipped=0;for(const raw of lines){const value=raw.trim();if(!value||value.startsWith('#'))continue;try{service.enqueue(value,queueId);imported++}catch{skipped++}}return{fileName:basename(path),imported,skipped,cancelled:false}});ipcMain.handle(IPC.exportList,async(_e,ids:string[])=>{const urls=[...new Set(ids.map(id=>service.get(id)?.url).filter((value):value is string=>Boolean(value)))];if(!urls.length)return{fileName:'',exported:0,cancelled:true};const picked=await dialog.showSaveDialog({title:'Export download list',buttonLabel:'Export',defaultPath:'downloads.txt',filters:[{name:'Text URL lists',extensions:['txt']}]});if(picked.canceled||!picked.filePath)return{fileName:'',exported:0,cancelled:true};writeFileSync(picked.filePath,`${urls.join('\n')}\n`,'utf8');return{fileName:basename(picked.filePath),exported:urls.length,cancelled:false}});ipcMain.handle(IPC.pause,(_e,id:string)=>service.pause(id));ipcMain.handle(IPC.resume,(_e,id:string)=>service.resume(id));ipcMain.handle(IPC.cancel,(_e,id:string)=>service.cancel(id));ipcMain.handle(IPC.deleteCompleted,()=>service.deleteCompleted());ipcMain.handle(IPC.open,(_e,id:string)=>{const item=service.get(id);return item?.savePath?shell.openPath(item.savePath):undefined});ipcMain.handle(IPC.showInFolder,(_e,id:string)=>{const item=service.get(id);if(item?.savePath)shell.showItemInFolder(item.savePath)});ipcMain.handle(IPC.copyUrl,(_e,id:string)=>{const item=service.get(id);if(item)clipboard.writeText(item.url)});ipcMain.handle(IPC.removeFromList,(_e,id:string)=>service.removeFromList(id));ipcMain.handle(IPC.deleteFromDisk,(_e,id:string)=>{const path=service.get(id)?.savePath;service.removeFromList(id);if(path&&existsSync(path))unlinkSync(path)});ipcMain.handle(IPC.getSegments,()=>service.getSegmentCount());ipcMain.handle(IPC.setSegments,(_e,value:number)=>service.setSegmentCount(value));ipcMain.handle(IPC.setItemSegments,(_e,id:string,value:number)=>service.setItemSegmentCount(id,value));ipcMain.handle(IPC.listQueues,()=>service.listQueues());ipcMain.handle(IPC.createQueue,(_e,name:string,concurrency:number)=>service.createQueue(name,concurrency));ipcMain.handle(IPC.updateQueue,(_e,id:string,name:string,concurrency:number)=>service.updateQueue(id,name,concurrency));ipcMain.handle(IPC.deleteQueue,(_e,id:string)=>service.deleteQueue(id));ipcMain.handle(IPC.startQueue,(_e,id:string)=>service.startQueue(id));ipcMain.handle(IPC.stopQueue,(_e,id:string)=>service.stopQueue(id));ipcMain.handle(IPC.openFolder,()=>shell.openPath(app.getPath('downloads')))}
export function broadcastDownloads(service:DownloadService){for(const window of BrowserWindow.getAllWindows())window.webContents.send(IPC.changed,service.list())}
